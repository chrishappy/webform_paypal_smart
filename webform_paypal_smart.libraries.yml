# Enable Smart Buttons on Webform
webform-paypal-checkout:
  version: 1
  js:
    assets/webform-paypal-checkout.js: {}
  dependencies:
    - core/jquery
    - webform_paypal_smart/dont-submit-again

# The following libraries are added through the .MODULE file
# - webform_paypal_smart/paypal-sdk
# - webform_paypal_smart/paypal-sdk--sandbox
# - webform_paypal_smart/paypal-sdk--live

# Prevent Clicking the Submit Button Twice
dont-submit-again:
  version: 1
  js:
    assets/dont-submit-again.js: {}
